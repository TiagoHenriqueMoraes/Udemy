
<div class="elegant-aero">
<script>
  $.notify("<%= notice %>", "success")
</script>

<h1>Varios Xesques</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @kinds.each do |kind| %>
      <tr>
        <td><%= kind.description %></td>
        <td><%= link_to 'Edit', edit_kind_path(kind), class: "btn btn-info"  %></td>
        <td><%= link_to 'Destroy', kind, method: :delete, class: "btn btn-danger",data: { confirm: 'Are you sure?' }%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Tipo', new_kind_path, class: "btn btn-success" %>
<%= link_to t('buttons.home'), root_path, class: "btn btn-default" %>

<div>